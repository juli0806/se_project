pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        stage('Build') {
            steps {
                bat 'javac -encoding UTF-8 Practice/src/Book.java'
		bat 'javac -encoding UTF-8 Practice/src/BookManager.java'
		bat 'javac -encoding UTF-8 Practice/src/BookManagerTest.java'
		bat 'javac -encoding UTF-8 Practice/src/PerformanceTest.java'
            }
        }
	stage('PerformanceTest') {
	    steps {
	        bat 'java -encoding UTF-8 Practice/src/PerformanceTest'
	    }
	}

    }
    
    post {
        always {
            archiveArtifacts 'test_results.txt'
        }
        failure {
            echo 'Build or test failed'
        }
        success {
            echo 'Build and test succeeded'
        }
    }
}
